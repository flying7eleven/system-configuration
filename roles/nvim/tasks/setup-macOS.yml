- name: Ensure neoVim and some additional tools are installed on the computer.
  ansible.builtin.homebrew:
    name: "{{ item }}"
    state: present
  with_items:
    - ctags
    - fzf
    - git
    - nvim
